spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://git.oschina.net/didispace/SpringBoot-Learning/
          searchPaths: Chapter9-1-4/config-repo
          username: username
          password: password
  profiles:
    active: native

server:
  port: 9501

